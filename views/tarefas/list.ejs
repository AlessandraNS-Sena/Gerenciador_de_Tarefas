<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Lista de Tarefas</title>
</head>
<body>
  <h1 style="text-align: center;">Tarefas</h1>
  <div style="text-align: center;">
    <a href="/tarefas/novo">➕ Nova Tarefa</a>
  </div>
  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Título</th>
        <th>Status</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% if (tarefas.length === 0) { %>
        <tr><td colspan="4" style="text-align: center;">Nenhuma tarefa encontrada.</td></tr>
      <% } else { %>
        <% tarefas.forEach(tarefa => { %>
          <tr>
            <td><%= tarefa.id %></td>
            <td><%= tarefa.titulo %></td>
            <td><%= tarefa.status %></td>
            <td>
              <a href="/tarefas/<%= tarefa.id %>">🔍 Ver</a>
              <a href="/tarefas/<%= tarefa.id %>/editar">✏️ Editar</a>
              <a href="/tarefas/<%= tarefa.id %>/deletar">🗑️ Excluir</a>
            </td>
          </tr>
        <% }) %>
      <% } %>
    </tbody>
  </table>
</body>
</html>
